import {useEffect,useState} from 'react';import {Navigate,Route,Routes} from 'react-router-dom';import {api} from './api/client';import {Layout} from './components/Layout';import {Loading} from './components/ui';import {DashboardPage} from './pages/DashboardPage';import {DailyPage} from './pages/DailyPage';import {HistoryPage} from './pages/HistoryPage';import {Login} from './pages/Login';import {StoriesPage} from './pages/StoriesPage';import {WeeklyPage} from './pages/WeeklyPage';import {TeamDaily} from './pages/TeamDailyPage';import {UsersPage} from './pages/UsersPage';import {PasswordPage,SettingsPage} from './pages/SettingsPages';import type {User} from './types';
export default function App(){const [user,setUser]=useState<User|null|undefined>(undefined);useEffect(()=>{if(!localStorage.getItem('token')){setUser(null);return}api<User>('/auth/me').then(setUser).catch(()=>setUser(null))},[]);if(user===undefined)return <Loading/>;if(!user)return <Routes><Route path="/login" element={<Login/>}/><Route path="*" element={<Navigate to="/login" replace/>}/></Routes>;const admin=user.role==='ADMIN';return <Layout user={user}><Routes><Route path="/" element={<DashboardPage isAdmin={admin} name={user.full_name}/>}/><Route path="/daily" element={admin?<Navigate to="/team-daily"/>:<DailyPage/>}/><Route path="/history" element={admin?<Navigate to="/"/>:<HistoryPage/>}/><Route path="/stories" element={<StoriesPage isAdmin={admin}/>}/><Route path="/password" element={<PasswordPage/>}/><Route path="/team-daily" element={admin?<TeamDaily/>:<Navigate to="/"/>}/><Route path="/users" element={admin?<UsersPage/>:<Navigate to="/"/>}/><Route path="/weekly" element={admin?<WeeklyPage/>:<Navigate to="/"/>}/><Route path="/settings" element={admin?<SettingsPage/>:<Navigate to="/"/>}/><Route path="*" element={<Navigate to="/"/>}/></Routes></Layout>}
