import {ReactNode,useState} from 'react';import {NavLink,useNavigate} from 'react-router-dom';import {BarChart3,BookOpenCheck,CalendarDays,ClipboardList,FileChartColumn,KeyRound,LogOut,Menu,Settings,Users,X} from 'lucide-react';import type {User} from '../types';
const member=[['/','Tổng quan cá nhân',BarChart3],['/daily','Daily của tôi',ClipboardList],['/history','Lịch sử Daily',CalendarDays],['/stories','User Stories',BookOpenCheck],['/password','Đổi mật khẩu',KeyRound]] as const;
const admin=[['/','Dashboard',BarChart3],['/team-daily','Daily của team',ClipboardList],['/stories','User Stories',BookOpenCheck],['/users','Thành viên',Users],['/weekly','Báo cáo tuần',FileChartColumn],['/password','Đổi mật khẩu',KeyRound],['/settings','Cài đặt',Settings]] as const;
export function Layout({user,children}:{user:User;children:ReactNode}){const [open,setOpen]=useState(false);const navigate=useNavigate();const items=user.role==='ADMIN'?admin:member;const logout=()=>{localStorage.removeItem('token');navigate('/login')};return <div className="min-h-screen lg:flex"><aside className={`fixed inset-y-0 left-0 z-30 w-72 bg-navy text-white transition-transform lg:sticky lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex h-20 items-center justify-between border-b border-white/10 px-6"><div><div className="text-xl font-extrabold tracking-tight">Daily<span className="text-orange-400">Flow</span></div><div className="text-xs text-slate-400">Team progress, rõ mỗi ngày</div></div><button onClick={()=>setOpen(false)} className="lg:hidden"><X/></button></div><nav className="space-y-1 p-4">{items.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-semibold ${isActive?'bg-white text-navy shadow':'text-slate-300 hover:bg-white/10 hover:text-white'}`}><Icon size={19}/>{label}</NavLink>)}</nav><div className="absolute bottom-0 w-full border-t border-white/10 p-4"><button onClick={logout} className="flex w-full items-center gap-3 rounded-xl px-4 py-3 text-sm text-slate-300 hover:bg-white/10"><LogOut size={18}/>Đăng xuất</button></div></aside><div className="min-w-0 flex-1"><header className="sticky top-0 z-20 flex h-20 items-center justify-between border-b border-stone-200 bg-paper/90 px-5 backdrop-blur lg:px-8"><button className="lg:hidden" onClick={()=>setOpen(true)}><Menu/></button><div className="hidden text-sm text-slate-500 lg:block">Asia/Ho_Chi_Minh · Dữ liệu cập nhật theo thời gian thực</div><div className="flex items-center gap-3"><div className="text-right"><div className="text-sm font-bold">{user.full_name}</div><div className="text-xs text-slate-500">{user.role==='ADMIN'?'Quản trị viên':'Thành viên'}</div></div><div className="grid h-10 w-10 place-items-center rounded-full bg-brand font-bold text-white">{user.full_name.charAt(0)}</div></div></header><main className="mx-auto max-w-[1500px] p-5 lg:p-8">{children}</main></div>{open&&<button aria-label="Đóng menu" className="fixed inset-0 z-20 bg-black/30 lg:hidden" onClick={()=>setOpen(false)}/>}</div>}
