import {FormEvent,useState} from 'react';import {ArrowRight,ChartNoAxesCombined,LockKeyhole} from 'lucide-react';import {api} from '../api/client';
export function Login(){const [username,setUsername]=useState('admin');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{const r=await api<{access_token:string}>('/auth/login',{method:'POST',body:JSON.stringify({username,password})});localStorage.setItem('token',r.access_token);location.href='/'}catch(e){setError(e instanceof Error?e.message:'Không thể đăng nhập')}finally{setLoading(false)}};return <div className="grid min-h-screen bg-white lg:grid-cols-2"><div className="relative hidden overflow-hidden bg-navy p-14 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute -right-32 -top-32 h-96 w-96 rounded-full border-[60px] border-white/5"/><div className="text-2xl font-extrabold">Daily<span className="text-orange-400">Flow</span></div><div className="relative"><ChartNoAxesCombined size={44} className="mb-7 text-orange-400"/><h1 className="max-w-lg text-5xl font-extrabold leading-tight">Ít họp hơn.<br/>Tiến độ rõ hơn.</h1><p className="mt-6 max-w-md text-lg leading-relaxed text-slate-300">Một nhịp làm việc minh bạch cho cả team — Daily gọn, blocker nổi bật, báo cáo tuần tự động.</p></div><div className="text-sm text-slate-400">Nội bộ · Bảo mật · Tập trung</div></div><div className="flex items-center justify-center bg-paper p-6"><form onSubmit={submit} className="card w-full max-w-md p-8 lg:p-10"><div className="mb-8"><div className="mb-5 inline-flex rounded-2xl bg-orange-100 p-3 text-brand"><LockKeyhole/></div><h2 className="text-3xl font-extrabold">Chào mừng trở lại</h2><p className="mt-2 text-slate-500">Đăng nhập để bắt đầu Daily hôm nay.</p></div><label className="label">Tên đăng nhập</label><input className="field mb-5" value={username} onChange={e=>setUsername(e.target.value)} autoFocus/><label className="label">Mật khẩu</label><input className="field" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="mt-3 text-sm font-medium text-red-600">{error}</p>}<button className="btn-primary mt-6 w-full" disabled={loading}>{loading?'Đang đăng nhập...':<>Đăng nhập <ArrowRight size={18}/></>}</button><p className="mt-6 text-center text-xs text-slate-400">Liên hệ Admin nếu bạn quên mật khẩu.</p></form></div></div>}

